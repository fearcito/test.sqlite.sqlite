tests:
    hello:
        assertStdoutEqual: "Hello World"
        input:
            - "Hello World"
software:
    python:
        execute: ["./sqlite3", "[[tests]]"]
        install:
            - ls -l
            - mkdir bld; cd bld; ../configure; make; make sqlite3.c
